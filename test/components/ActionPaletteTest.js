var React = require('react/addons'),
    assert = require('assert'),
    ActionPalette = require('../../src/components/ActionPalette.jsx'),
    TestUtils = React.addons.TestUtils;

describe('ActionPalette component', function(){
  //TODO: Need to accept props which allow for easier styling
  it('placeholder:does nothing', function() {
    assert(true);
  });
});

